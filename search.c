#include <stdio.h>
#include <string.h>
#include <stdlib.h>

static int searchInsert(int* nums, int numsSize, int target)
{
    printf("this the target: %d\n", target);
    for (int i = 0; i < numsSize; i++)
    {
        
    }
}

int main( void )
{
    int num[] = {1, 3, 5, 6};
    int target = 3;
    searchInsert(num, 4, target);
}